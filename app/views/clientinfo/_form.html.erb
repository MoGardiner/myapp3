<%= form_for @clientinfo, url: url, method: method do |f| %>
  
  <% if @clientinfo.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@clientinfo.errors.count, "error") %> prohibited this client from being saved:
      </h2>
      <ul>
        <% @clientinfo.errors.full_messages.each do |msg| %>
         <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  <p>
  
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  <p>
  
  <p>
    <%= f.submit %>
  <p>

<% end %>
